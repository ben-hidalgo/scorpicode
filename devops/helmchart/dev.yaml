# dev values

common:
  enabled: false

frontend:
  enabled: false

hats:
  enabled: false

website:
  enabled: false

roxie:
  enabled: false

soxie:
  enabled: false
  
debugger:
  enabled: false
  
ingress:
  enabled: false

nginx:
  enabled: false

mongodb:
  enabled: true
  mongodbUsername: scuser
  mongodbPassword: scpass
  mongodbDatabase: scdata
  service:
    type: LoadBalancer

rabbitmq-ha:
  enabled: true
  managementUsername: scuser
  managementPassword: scpass
  rabbitmqUsername: scuser
  rabbitmqPassword: scpass
  rabbitmqVhost: schost
  replicaCount: 0
  service:
    type: LoadBalancer
  prometheus:
    operator:
      enabled: false

redis:
  enabled: false
